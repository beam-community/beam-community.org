---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="text-center mb-12">
  <h2 class="text-3xl font-bold text-slate-900 sm:text-4xl">{title}</h2>
  {subtitle && <p class="mt-4 text-lg text-slate-600 max-w-2xl mx-auto">{subtitle}</p>}
</div>
