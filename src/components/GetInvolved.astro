---
import { Icon } from "astro-icon/components";
import { INVOLVEMENT_CARDS } from "../lib/constants";
import SectionHeading from "./SectionHeading.astro";
---

<section id="get-involved" class="py-20 sm:py-24 bg-white dark:bg-slate-900">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <SectionHeading
      title="Get Involved"
      subtitle="The BEAM community thrives because of contributors like you."
    />

    <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-8">
      {
        INVOLVEMENT_CARDS.map((card) => (
          <div class="text-center p-8 rounded-xl border border-slate-200 dark:border-slate-700 hover:shadow-lg transition-shadow dark:bg-slate-800">
            <div class="inline-flex items-center justify-center w-14 h-14 rounded-full bg-purple-100 dark:bg-purple-950/50 mb-5">
              <Icon name={card.icon} class="w-7 h-7 text-purple-600 dark:text-purple-400" />
            </div>
            <h3 class="text-lg font-semibold text-slate-900 dark:text-white mb-2">{card.title}</h3>
            <p class="text-sm text-slate-600 dark:text-slate-400 mb-5">{card.description}</p>
            <a
              href={card.linkUrl}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex items-center gap-1.5 text-sm font-medium text-purple-700 dark:text-purple-400 hover:text-purple-900 dark:hover:text-purple-300 transition-colors"
            >
              {card.linkText}
              <Icon name="lucide:arrow-right" class="w-4 h-4" />
            </a>
          </div>
        ))
      }
    </div>
  </div>
</section>
